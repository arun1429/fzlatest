import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  bannerAds: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    // height: 100,
    marginVertical: 10,
    marginTop: 16,
  },
});
